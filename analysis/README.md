# Analysis

Use this folder for shared scripts, notebooks, and utilities that are not tied to one project.

Suggested subfolders, once needed:

```text
scripts/
notebooks/
utilities/
```

Project-specific analysis should usually live inside the relevant folder in `projects/`.
