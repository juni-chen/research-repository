# Data

Use this folder for data records and dataset documentation.

Recommended subfolders, once needed:

```text
raw/
processed/
annotations/
metadata/
```

Keep sensitive, private, or copyrighted data out of Git unless you have permission to store it. Use `external-links.md` for datasets stored elsewhere.
