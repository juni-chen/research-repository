# Outputs

The real CGN experiment writes results here by default:

```text
outputs/cgn-run/
```

Generated outputs are ignored by Git. Keep only small documentation files in
this folder unless a result is safe and useful to publish.
